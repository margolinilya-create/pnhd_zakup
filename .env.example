# DEPRECATED. This root .env.example only fed the local Docker Compose Postgres,
# which has been removed — the database is now Supabase (managed Postgres).
#
# There is no root .env to create. Put the database connection strings in
# backend/.env (copy from backend/.env.example) and follow docs/LOCAL_DATABASE.md.
